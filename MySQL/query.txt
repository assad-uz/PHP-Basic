Q: Query বলতে কী বোঝায়?
A: Query (কুয়েরি) শব্দের মানে হলো: প্রশ্ন করা বা অনুরোধ করা।

- ডেটাবেইসের দুনিয়ায়, “Query” মানে হলো—আপনি ডেটাবেইসকে কোনো তথ্য চাইছেন, বা কোনো কাজ করতে বলছেন।

> সহজ ভাষায়:
Query = ডেটাবেইসকে নির্দেশ দেওয়া
যেমন: “এই ইউজারটা যোগ করো”, “এই ইউজারকে মুছে ফেলো”, “এই ইউজারের নাম দেখাও” — এসবই Query।

-------------------------
> উদাহরণ দিয়ে বোঝানো যাক:

1. ডেটা যোগ করার Query (Insert)

INSERT INTO users (name, email) VALUES ('Asad', 'asad@example.com');

মানে: "users টেবিলে একটি নতুন ইউজার যোগ করো যার নাম Asad এবং ইমেইল asad@example.com"

2. 🧹 ডেটা মুছে ফেলার Query (Delete)
sql
Copy
Edit
DELETE FROM users WHERE id = 3;
মানে:

"যে ইউজারের id ৩, তাকে মুছে ফেলো।"

3. 🔄 ডেটা আপডেট করার Query (Update)
sql
Copy
Edit
UPDATE users SET name = 'Hasan' WHERE id = 5;
মানে:

"যে ইউজারের id ৫, তার নাম বদলে 'Hasan' করো।"

4. 🔍 ডেটা দেখার Query (Select)
sql
Copy
Edit
SELECT * FROM users;
মানে:

"users টেবিল থেকে সব ইউজারের তথ্য দেখাও।"

🔚 উপসংহার:
Query হচ্ছে SQL ভাষায় লেখা একটি আদেশ যেটা ডেটাবেইসের কাছে পাঠানো হয় কোনো তথ্য আনতে, যোগ করতে, মুছতে, বা আপডেট করতে।