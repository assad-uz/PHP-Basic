Q: ALTER কী?
A:
🔹 ALTER মানে হচ্ছে পরিবর্তন করা (Modify/Change)।
🔹 এটি ব্যবহার করে আপনি আগে থেকে তৈরি করা টেবিলে নতুন কিছু যোগ করতে, কিছু বাদ দিতে, বা পরিবর্তন করতে পারেন।

----------------------------------------
Ex: ALTER দিয়ে টেবিলে নতুন কলাম যোগ করা

ALTER TABLE users
ADD age INT;
👉 users টেবিলে age নামে নতুন একটি কলাম যোগ হলো।

----------------------------------------
Ex: ALTER দিয়ে প্রাইমারি কী যোগ করা

ALTER TABLE users
ADD PRIMARY KEY (id);
👉 users টেবিলের id কলামকে প্রাইমারি কী বানিয়ে দিল।


----------------------------------------
✅ উদাহরণ: ALTER দিয়ে কলাম মুছে ফেলা

ALTER TABLE users
DROP COLUMN age;
👉 users টেবিল থেকে age কলাম মুছে যাবে।

